# GPU-specific requirements for SAM 2 inference

# PyTorch with CUDA support
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2

# CUDA dependencies
nvidia-cuda-runtime-cu12==12.1.105
nvidia-cudnn-cu12==8.9.7.29

# Image processing
opencv-python==4.9.0.80

# SAM 2 dependencies (will be installed via git)
# git+https://github.com/facebookresearch/sam2.git

# Optional: CLIP for classification
# clip @ git+https://github.com/openai/CLIP.git
